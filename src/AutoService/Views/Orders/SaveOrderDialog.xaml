<Window x:Class="AutoService.Views.Orders.SaveOrderDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Заказ" Height="600" Width="700"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Номер заказа -->
        <Label Grid.Row="0" Grid.Column="0" Content="Номер заказа:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding OrderNumber}" 
                 IsReadOnly="True" Background="LightGray" Margin="5"/>

        <!-- Клиент -->
        <Label Grid.Row="1" Grid.Column="0" Content="Клиент:" VerticalAlignment="Center"/>
        <ComboBox Grid.Row="1" Grid.Column="1" 
                  ItemsSource="{Binding Customers}"
                  DisplayMemberPath="FullName"
                  SelectedItem="{Binding SelectedCustomer}"
                  Margin="5"/>

        <!-- Услуги -->
        <Label Grid.Row="2" Grid.Column="0" Content="Услуги:" VerticalAlignment="Top" Margin="0,5,0,0"/>
        <Grid Grid.Row="2" Grid.Column="1" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="200"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
                <ComboBox x:Name="ServicesCombo" 
                          ItemsSource="{Binding AvailableServices}"
                          DisplayMemberPath="Name"
                          SelectedItem="{Binding SelectedService}"
                          Width="200" Margin="0,0,5,0"/>
                <Button Content="Добавить услугу" Command="{Binding AddServiceCommand}" 
                        Padding="10,2"/>
            </StackPanel>

            <DataGrid Grid.Row="1" ItemsSource="{Binding SelectedServices}"
                      SelectedItem="{Binding SelectedOrderService}"
                      AutoGenerateColumns="False" CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Услуга" Binding="{Binding Service.Name}" Width="*"/>
                    <DataGridTextColumn Header="Цена" Binding="{Binding Price}" Width="100"/>
                    <DataGridTemplateColumn Header="Действия" Width="70">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Удалить" 
                                        Command="{Binding DataContext.RemoveServiceCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!-- Сотрудники -->
        <Label Grid.Row="3" Grid.Column="0" Content="Сотрудники:" VerticalAlignment="Top" Margin="0,5,0,0"/>
        <Grid Grid.Row="3" Grid.Column="1" Margin="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="150"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
                <ComboBox x:Name="EmployeesCombo" 
                          ItemsSource="{Binding AvailableEmployees}"
                          DisplayMemberPath="FullName"
                          SelectedItem="{Binding SelectedEmployee}"
                          Width="200" Margin="0,0,5,0"/>
                <Button Content="Добавить сотрудника" Command="{Binding AddEmployeeCommand}" 
                        Padding="10,2"/>
            </StackPanel>


            <DataGrid Grid.Row="1" ItemsSource="{Binding SelectedEmployees}"
                      SelectedItem="{Binding SelectedEmployeeForRemove}"
                      AutoGenerateColumns="False" CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Имя" Binding="{Binding FullName}" Width="*"/>
                    <DataGridTemplateColumn Header="Действия" Width="70">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Удалить" 
                                        Command="{Binding DataContext.RemoveEmployeeCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!-- Сумма -->
        <Label Grid.Row="4" Grid.Column="0" Content="Итого:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding TotalAmount, StringFormat=C}" 
                 IsReadOnly="True" Background="LightGray" Margin="5"/>

        <!-- Примечание -->
        <Label Grid.Row="5" Grid.Column="0" Content="Примечание:" VerticalAlignment="Top" Margin="0,5,0,0"/>
        <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding Note}" 
                 Height="60" TextWrapping="Wrap" Margin="5" AcceptsReturn="True"/>

        <!-- Кнопки -->
        <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Сохранить" Command="{Binding SaveCommand}" Padding="20,5" Margin="0,0,10,0" IsDefault="True"/>
            <Button Content="Отмена" Command="{Binding CancelCommand}" Padding="20,5" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>