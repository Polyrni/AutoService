<UserControl x:Class="AutoService.Views.Orders.OrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <DockPanel>
        <ToolBarTray DockPanel.Dock="Top">
            <ToolBar>
                <Button Content="Обновить" Command="{Binding RefreshCommand}" />
                <Button Content="Добавить" Command="{Binding AddCommand}" />
                <Button Content="Изменить" Command="{Binding EditCommand}" />
                <Button Content="Удалить" Command="{Binding DeleteCommand}" />
            </ToolBar>
        </ToolBarTray>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Orders}" 
                  SelectedItem="{Binding SelectedOrder}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  AlternatingRowBackground="LightGray" 
                  AlternationCount="2">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Номер заказа" Binding="{Binding OrderNumber}" Width="120"/>
                <DataGridTextColumn Header="Дата создания" Binding="{Binding CreatedAt, StringFormat='dd.MM.yyyy HH:mm'}" Width="150"/>
                <DataGridTextColumn Header="Дата изменения" Binding="{Binding UpdatedAt, StringFormat='dd.MM.yyyy HH:mm'}" Width="150"/>
                <DataGridTextColumn Header="Клиент" Binding="{Binding Customer.FullName}" Width="200"/>
                <DataGridTextColumn Header="Сумма" Binding="{Binding TotalAmount, StringFormat=C}" Width="100"/>
                <DataGridTextColumn Header="Примечание" Binding="{Binding Note}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>